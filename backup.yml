#Ansible Playbook
#Operated from Central Node


- name: Execute Backup Script on All Servers
  hosts: servers
  become: yes

  tasks:
    - name: Run backup script
      shell: /home/user/backup.sh



# Test connection
ansible -i hosts.ini servers -m ping

# Run playbook
ansible-playbook -i hosts.ini backup.yml
